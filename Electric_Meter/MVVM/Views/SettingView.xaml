<UserControl x:Class="Electric_Meter.MVVM.Views.SettingView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:Electric_Meter.Converters"
			 mc:Ignorable="d"
			 d:DesignWidth="900" Height="700"
			 Background="#FFF">
    



    <Grid Margin="5">

        <Grid.RowDefinitions>
            <RowDefinition Height="320"/>
            <RowDefinition Height="380"/>
        </Grid.RowDefinitions>
        <Border  Grid.Row="0" Style="{StaticResource BorderContent}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{Binding DetailDeviceText}"
                                Style="{StaticResource H3}"
                                />

                    <Border Grid.Row="1" Style="{StaticResource BorderContent}">
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding DevidCommandText}" Style="{StaticResource  Content Textblock}"/>
                                    <TextBox  Text="{Binding Devid,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Content TextBox}"
                                />
                                 </StackPanel>
                                <StackPanel Orientation="Horizontal" Grid.Row="1">
                                    <TextBlock Text="{Binding NameDeviceCommandText}" Style="{StaticResource  Content Textblock}"/>
                                    <TextBox  Text="{Binding NameDevice, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource Content TextBox}"
                                    />
                                </StackPanel>
                            </Grid>
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding ActiveCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
	                                    ItemsSource="{Binding LstActive}"
	                                    SelectedItem="{Binding SelectedActive,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource Content Combobox}"
	                                    />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Grid.Row="1">
                                    <TextBlock Text="{Binding SenSorTypeCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
                                        ItemsSource="{Binding LstSensorType}"
                                        SelectedItem="{Binding SelectedSensorType,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource Content Combobox}"
                                        />
                                </StackPanel>
                            </Grid>
                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>
                                
                            </Grid>
                            
                        </Grid>
                    </Border>
                    <TextBlock Grid.Row="2" Text="{Binding ListDeviceText}"
                                Style="{StaticResource H3}"
                                />
                    <DataGrid x:Name="DeviceGrid" Style="{StaticResource BaseDataGridView}"
                                            Grid.Row="3"
					                        ItemsSource="{Binding DeviceList, UpdateSourceTrigger=PropertyChanged}"
					                        SelectedItem="{Binding SelectedDevice, UpdateSourceTrigger=PropertyChanged}"
					                        >

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{Binding DevidCommandText}" Binding="{Binding devid}" Width="*"/>
                            <DataGridTextColumn Header="{Binding NameDeviceCommandText}" Binding="{Binding name}" Width="*"/>
                            <DataGridTextColumn Header="{Binding ActiveCommandText}" Binding="{Binding active}" Width="*"/>
                            <DataGridTextColumn Header="{Binding TypeCommandText}" Binding="{Binding type}" Width="*"/>
                            <DataGridTextColumn Header="{Binding IfShowCommandText}" Binding="{Binding ifshow}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
                <StackPanel Grid.Column="1"
	Orientation="Vertical"
	HorizontalAlignment="Center"
	VerticalAlignment="Top"
	Margin="10,15,0,0">

                    <!-- Áp dụng Style mới cho các nút bên phải -->
                    <Button Content="{Binding AddDeviceCommandText}"
	Command="{Binding AddDeviceCommand}"
	ToolTip="{Binding AddDeviceCommandText}"
	Margin="0,0,0,10"
	/>

                    <Button Content="{Binding EditDeviceCommandText}"
	Command="{Binding EditDeviceCommand}"
	ToolTip="{Binding EditDeviceCommandText}"
	IsEnabled="{Binding IsEnableBtnEditDevice}" Margin="0,0,0,10"
	/>

                    <Button Content="{Binding DeleteDeviceCommandText}"
	Command="{Binding DeleteDeviceCommand}"
	ToolTip="{Binding DeleteDeviceCommandText}"
	/>

                </StackPanel>
            </Grid>

        </Border>
        <Border  Grid.Row="1" Style="{StaticResource BorderContent}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="2" Text="{Binding DetailControlCodeText}"
                    Style="{StaticResource H3}"
                    />
                    <Border Grid.Row="3" Style="{StaticResource BorderContent}">
                        <Grid Margin="5">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- CỘT 1 -->
                            <Grid Grid.Column="0" Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>

                                <!-- CodeId -->
                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding CodeIdCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding CodeId}" Style="{StaticResource Content TextBox}" TextChanged="TextBox_TextChanged"/>
                                </StackPanel>

                                <!-- DevId -->
                                <StackPanel Orientation="Horizontal" Grid.Row="1">
                                    <TextBlock Text="{Binding NameDeviceCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
                                        ItemsSource="{Binding LstDeviceControlcode}"
                                        SelectedItem="{Binding SelectedDeviceName,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource Content Combobox}"
                                        />
                                </StackPanel>

                                <!-- Code -->
                                <StackPanel Orientation="Horizontal" Grid.Row="2">
                                    <TextBlock Text="{Binding CodeCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding Code}" Style="{StaticResource Content TextBox}"/>
                                </StackPanel>

                                <!-- ActiveId -->
                                <StackPanel Orientation="Horizontal" Grid.Row="3">
                                    <TextBlock Text="{Binding ActiveCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
                                        ItemsSource="{Binding LstActiveControlcode}"
                                        SelectedItem="{Binding SelectedActiveControlcodeName,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource Content Combobox}"
                                        />
                                </StackPanel>
                            </Grid>

                            <!-- CỘT 2 -->
                            <Grid Grid.Column="1" Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding SenSorTypeCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
                                        ItemsSource="{Binding LstCodeType}"
                                        SelectedItem="{Binding SelectedCodeType,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Style="{StaticResource Content Combobox}"
                                        />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Grid.Row="1">
                                    <TextBlock Text="{Binding NameCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding NameControlcode}" Style="{StaticResource Content TextBox}"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Grid.Row="2">
                                    <TextBlock Text="{Binding FactorCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding Factor}" Style="{StaticResource Content TextBox}"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Grid.Row="3">
                                    <TextBlock Text="{Binding NameCodeTypeCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <ComboBox 
                                        ItemsSource="{Binding LstSensorTypeControlCode}"
                                        SelectedItem="{Binding SelectedSensorTypeControlCode,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource Content Combobox}"
                                        />
                                </StackPanel>
                            </Grid>

                            <!-- CỘT 3 -->
                            <Grid Grid.Column="2" Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <TextBlock Text="{Binding HighCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding High}" Style="{StaticResource Content TextBox}"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Grid.Row="1">
                                    <TextBlock Text="{Binding LowCommandText}" VerticalAlignment="Center" Style="{StaticResource Content Textblock}"/>
                                    <TextBox  Text="{Binding Low}" Style="{StaticResource Content TextBox}"/>
                                </StackPanel>
                                
                            </Grid>

                        </Grid>

                    </Border>
                    <TextBlock Grid.Row="0" Text="{Binding ListControlCodeText}"
            Style="{StaticResource H3}"
            />
                    <DataGrid x:Name="ControlcodeGrid"  Style="{StaticResource BaseDataGridView}"
                        Grid.Row="1"
					    ItemsSource="{Binding ControlCodeList, UpdateSourceTrigger=PropertyChanged}"
					    
					    SelectedItem="{Binding SelectedControlCode, UpdateSourceTrigger=PropertyChanged}"
					    >
                        
    <DataGrid.Columns>
                            <DataGridTextColumn Header="{Binding CodeIdCommandText}" Binding="{Binding CodeId}" Width="*"/>
                            <DataGridTextColumn Header="{Binding NameDeviceCommandText}" Binding="{Binding DeviceName}" Width="*"/>
                            <DataGridTextColumn Header="{Binding CodeCommandText}" Binding="{Binding Code}" Width="*"/>
                            <DataGridTextColumn Header="{Binding ActiveCommandText}" Binding="{Binding Active}" Width="*"/>
                            <DataGridTextColumn Header="{Binding SenSorTypeCommandText}" Binding="{Binding SensorType}" Width="*"/>
                            <DataGridTextColumn Header="{Binding NameCodeTypeCommandText}" Binding="{Binding CodeType}" Width="*"/>
                            <DataGridTextColumn Header="{Binding NameCommandText}" Binding="{Binding NameControlcode}" Width="*"/>
                            <DataGridTextColumn Header="{Binding FactorCommandText}" Binding="{Binding Factor}" Width="*"/>
                            <DataGridTextColumn Header="{Binding HighCommandText}" Binding="{Binding High}" Width="*"/>
                            <DataGridTextColumn Header="{Binding LowCommandText}" Binding="{Binding Low}" Width="*"/>
                        </DataGrid.Columns>

                    </DataGrid>
                </Grid>
                <StackPanel Grid.Column="1"
	Orientation="Vertical"
	HorizontalAlignment="Center"
	VerticalAlignment="Top"
	Margin="10,15,0,0">

                    <!-- Áp dụng Style mới cho các nút bên phải -->
                    <Button Content="{Binding AddControlCodeCommandText}"
	Command="{Binding AddControlCodeCommand}"
	ToolTip="{Binding AddControlCodeCommandText}"
	Margin="0,0,0,10"
	/>

                    <Button Content="{Binding EditControlCodeCommandText}"
	Command="{Binding EditControlCodeCommand}"
	ToolTip="{Binding EditControlCodeCommandText}"
	IsEnabled="{Binding IsEnableBtnEditControlCode}" Margin="0,0,0,10"
	/>

                    <Button Content="{Binding DeleteControlCodeCommandText}"
	Command="{Binding DeleteControlCodeCommand}"
	ToolTip="{Binding DeleteControlCodeCommandText}"
	/>

                </StackPanel>
            </Grid>

        </Border>

    </Grid>


</UserControl>
