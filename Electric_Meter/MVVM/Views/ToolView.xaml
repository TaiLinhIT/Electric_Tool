<UserControl x:Class="Electric_Meter.MVVM.Views.ToolView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
			 xmlns:local="clr-namespace:Electric_Meter.MVVM.Views"
			 mc:Ignorable="d"
             MinHeight="480"
             MinWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Border Margin="16"
			BorderThickness="0.5"
			CornerRadius="10" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
                    <ui:SymbolIcon Symbol="Clock24" Margin="0,0,6,0"  FontSize="14"/>
                    <TextBlock Text="{Binding CurrentTime, StringFormat='HH:mm:ss dd/MM/yyyy'}"
								   FontSize="12"
								   FontWeight="Medium"
								   VerticalAlignment="Center"
								   />
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <!-- Device ComboBox: Đã thêm Padding cho văn bản của mục được chọn -->
                    <ComboBox Grid.Column="0"
								  ItemsSource="{Binding LstDevice}"
								  DisplayMemberPath="name"
								  SelectedValuePath="devid"
								  SelectedItem="{Binding SelectedDevice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
								  Height="32"
								  VerticalContentAlignment="Center"
								  Padding="10,0,10,0"
								  HorizontalAlignment="Left"/>

                    <ComboBox Grid.Column="2"
								  ItemsSource="{Binding LstAssembling}"
								  DisplayMemberPath="value"
								  SelectedValuePath="key"
								  SelectedItem="{Binding SelectedAssembling,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
								  VerticalContentAlignment="Center"
								  Padding="10,0,10,0"
								  Height="32"
								  HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Khối bên trái: Voltage và Current -->
                <Grid Grid.Column="0" Margin="0, 0, 10, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding PhaseAVoltageText}" />
                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Ua}"  />
                        <TextBlock Text="V"  />
                    </StackPanel>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding PhaseBVoltageText}" />
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Ub}"  />
                        <TextBlock Text="V"  />
                    </StackPanel>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding PhaseCVoltageText}" />
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Uc}"  />
                        <TextBlock Text="V"  />
                    </StackPanel>

                    <Rectangle Grid.Row="3" Grid.ColumnSpan="2" Height="1"  Margin="0,15"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="{Binding PhaseACurrentText}" />
                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Ia}"  />
                        <TextBlock Text="A"  />
                    </StackPanel>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="{Binding PhaseBCurrentText}" />
                    <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Ib}"  />
                        <TextBlock Text="A"  />
                    </StackPanel>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="{Binding PhaseCCurrentText}" />
                    <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Ic}"  />
                        <TextBlock Text="A"  />
                    </StackPanel>

                </Grid>

                <!-- Khối bên phải: Power và Energy -->
                <Grid Grid.Column="2" Margin="10, 0, 0, 0">
                    <Grid.ColumnDefinitions>
                        <!-- Đã sửa: Thay Width="*" bằng Width="auto" để cột nhãn chỉ rộng bằng nội dung -->
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding TotalPowerText}" />
                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Pt}"  />
                        <TextBlock Text="kW"  />
                    </StackPanel>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding PhaseAPowerText}" />
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Pa}"  />
                        <TextBlock Text="kW"  />
                    </StackPanel>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding PhaseBPowerText}" />
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Pb}"  />
                        <TextBlock Text="kW"  />
                    </StackPanel>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding PhaseCPowerText}" />
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Pc}"  />
                        <TextBlock Text="kW"  />
                    </StackPanel>

                    <Rectangle Grid.Row="4" Grid.ColumnSpan="2" Height="1"  Margin="0,15"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="{Binding ExportEnergyText}" />
                    <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Exp}"  />
                        <TextBlock Text="kWh"  />
                    </StackPanel>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="{Binding ImportEnergyText}" />
                    <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Imp}"  />
                        <TextBlock Text="kWh"  />
                    </StackPanel>

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="{Binding TotalEnergyText}" />
                    <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Total}"  />
                        <TextBlock Text="kWh"  />
                    </StackPanel>

                </Grid>
            </Grid>

            <Border Grid.RowSpan="2"
					Opacity="0.8"
					Panel.ZIndex="100"
					Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ui:ProgressRing IsIndeterminate="True"
								 Width="50"
								 Height="50"
								 
								 HorizontalAlignment="Center"
								 VerticalAlignment="Center"/>
            </Border>

        </Grid>
    </Border>
</UserControl>
